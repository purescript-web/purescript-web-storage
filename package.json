{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "jshint src && jscs src && pulp build"
  },
  "devDependencies": {
    "bower": "^1.8.0",
    "jscs": "^3.0.7",
    "jshint": "^2.9.4",
    "pulp": "^10.0.0",
    "purescript": "^0.10.3",
    "purescript-psa": "^0.4.0"
  }
}
